class Solution {
    public int solution(int[] numbers, int n) {
        int answer = 0;
        for(int a : numbers) {
            if( answer > n) break;
            answer += a;
        }
        return answer;
    }
}
