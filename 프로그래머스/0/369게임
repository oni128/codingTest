class Solution {
    public int solution(int order) {
        int answer = 0;
        String a = String.valueOf(order);
        for(int i=0; i<a.length(); i++) {
            if("369".contains(String.valueOf(a.charAt(i)))){
                answer += 1;
            }
        }
        return answer;
    }
}
