class Solution{
    // 참가자 수, 참가자 번호, 경쟁자 번호 // 몇라운드에 만나는지 출력
    public int solution(int n, int a, int b){

         int count = 0;

        while (a != b) {
            a = (a + 1) / 2;
            b = (b + 1) / 2;
            count++;
        }

        return count;
    }
}
